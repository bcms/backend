import { createGraphqlObject } from '@becomes/purple-cheetah-mod-graphql';

export const BCMSPropDataValueGroupPointerObject = createGraphqlObject({
  name: 'BCMSPropDataValueGroupPointer',
  fields: {
    _id: 'String!',
  },
});
