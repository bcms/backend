import { createGraphqlObject } from '@becomes/purple-cheetah-mod-graphql';

export const BCMSPropDataValueWidgetObject = createGraphqlObject({
  name: 'BCMSPropDataValueWidget',
  fields: {
    _id: 'String!',
  },
});
