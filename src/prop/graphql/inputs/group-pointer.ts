import { createGraphqlInput } from '@becomes/purple-cheetah-mod-graphql';

export const BCMSPropDataValueGroupPointerInput = createGraphqlInput({
  name: 'BCMSPropGroupPointerInput',
  fields: {
    _id: 'String!',
  },
});
